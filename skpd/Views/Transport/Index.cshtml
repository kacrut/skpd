@model IEnumerable<skpd.Models.Transport>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            TransportName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TransportName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransportID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransportID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransportID })
        </td>
    </tr>
}

</table>

@model skpd.Models.Position

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Position</legend>

    <div class="display-label">PositionName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PositionName)
    </div>

    <div class="display-label">Division</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Division.DivisionName)
    </div>

    <div class="display-label">Position3</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Position3.PositionName)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
